---
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This code is generated by Magic Modules using the following:
#
#     Configuration: https:#github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/products/compute/RegionNetworkPolicy.yaml
#     Template:      https:#github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/templates/terraform/resource.html.markdown.tmpl
#
#     DO NOT EDIT this file directly. Any changes made to this file will be
#     overwritten during the next generation cycle.
#
# ----------------------------------------------------------------------------
subcategory: "Compute Engine"
description: |-
  Represents a Compute Region Network Policy resource.
---

# google_compute_region_network_policy

Represents a Compute Region Network Policy resource.



## Example Usage - Region Network Policy Basic


```hcl
resource "google_compute_region_network_policy" "default" {
  name        = "tf-test-policy-basic"
  region      = "us-west1"
  project     = "my-project-name"
  description = "A basic region network policy"
}
```
## Example Usage - Region Network Policy Full


```hcl
resource "google_compute_region_network_policy" "default" {
  name        = "tf-test-policy-full"
  region      = "us-west1"
  project     = "my-project-name"
  description = "A full region network policy"

  traffic_classification_rules {
    rule_name = "rule1"
    priority  = 1000
    match {
      src_ip_ranges = ["10.0.0.0/8"]
      dest_ip_ranges = ["192.168.0.0/16"]
      layer4_configs {
        ip_protocol = "TCP"
        ports       = ["80", "8080"]
      }
    }
    action {
      type = "ALLOW"
    }
    disabled = false
  }
}
```

## Argument Reference

The following arguments are supported:


* `name` -
  (Required)
  Name of the resource.


* `description` -
  (Optional)
  An optional description of this resource.

* `traffic_classification_rules` -
  (Optional)
  List of traffic classification rules.
  Structure is [documented below](#nested_traffic_classification_rules).

* `region` -
  (Optional)
  The region of this resource.

* `project` - (Optional) The ID of the project in which the resource belongs.
    If it is not provided, the provider project is used.

* `deletion_policy` - (Optional) Whether Terraform will be prevented from destroying the resource. Defaults to DELETE.
	When a 'terraform destroy' or 'terraform apply' would delete the resource,
	the command will fail if this field is set to "PREVENT" in Terraform state.
	When set to "ABANDON", the command will remove the resource from Terraform
	management without updating or deleting the resource in the API.
	When set to "DELETE", deleting the resource is allowed.


<a name="nested_traffic_classification_rules"></a>The `traffic_classification_rules` block supports:

* `rule_name` -
  (Optional)
  Name for the rule.

* `priority` -
  (Optional)
  Rule priority.

* `match` -
  (Optional)
  Match condition.
  Structure is [documented below](#nested_traffic_classification_rules_match).

* `action` -
  (Optional)
  Action to take.
  Structure is [documented below](#nested_traffic_classification_rules_action).

* `target_service_accounts` -
  (Optional)

* `target_secure_tags` -
  (Optional)
  Structure is [documented below](#nested_traffic_classification_rules_target_secure_tags).

* `disabled` -
  (Optional)


<a name="nested_traffic_classification_rules_match"></a>The `match` block supports:

* `src_ip_ranges` -
  (Optional)

* `dest_ip_ranges` -
  (Optional)

* `layer4_configs` -
  (Optional)
  Structure is [documented below](#nested_traffic_classification_rules_match_layer4_configs).


<a name="nested_traffic_classification_rules_match_layer4_configs"></a>The `layer4_configs` block supports:

* `ip_protocol` -
  (Optional)

* `ports` -
  (Optional)

<a name="nested_traffic_classification_rules_action"></a>The `action` block supports:

* `type` -
  (Optional)

* `traffic_class` -
  (Optional)
  Possible values are: .

* `dscp_mode` -
  (Optional)
  Possible values are: .

* `dscp_value` -
  (Optional)

<a name="nested_traffic_classification_rules_target_secure_tags"></a>The `target_secure_tags` block supports:

* `name` -
  (Optional)

* `state` -
  (Optional)
  Possible values are: .

## Attributes Reference

In addition to the arguments listed above, the following computed attributes are exported:

* `id` - an identifier for the resource with format `projects/{{project}}/regions/{{region}}/networkPolicies/{{name}}`

* `region_network_policy_id` -
  The unique identifier for the resource.


## Timeouts

This resource provides the following
[Timeouts](https://developer.hashicorp.com/terraform/plugin/sdkv2/resources/retries-and-customizable-timeouts) configuration options:

- `create` - Default is 20 minutes.
- `update` - Default is 20 minutes.
- `delete` - Default is 20 minutes.

## Import


RegionNetworkPolicy can be imported using any of these accepted formats:

* `projects/{{project}}/regions/{{region}}/networkPolicies/{{name}}`
* `{{project}}/{{region}}/{{name}}`
* `{{region}}/{{name}}`
* `{{name}}`

In Terraform v1.12.0 and later, use an [`identity` block](https://developer.hashicorp.com/terraform/language/block/import#identity) to import RegionNetworkPolicy using identity values. For example:

```tf
import {
  identity = {
    name = "<-required value->"
    region = "<-optional value->"
    project = "<-optional value->"
  }
  to = google_compute_region_network_policy.default
}
```

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import) to import RegionNetworkPolicy using one of the formats above. For example:

```tf
import {
  id = "projects/{{project}}/regions/{{region}}/networkPolicies/{{name}}"
  to = google_compute_region_network_policy.default
}
```

When using the [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import), RegionNetworkPolicy can be imported using one of the formats above. For example:

```
$ terraform import google_compute_region_network_policy.default projects/{{project}}/regions/{{region}}/networkPolicies/{{name}}
$ terraform import google_compute_region_network_policy.default {{project}}/{{region}}/{{name}}
$ terraform import google_compute_region_network_policy.default {{region}}/{{name}}
$ terraform import google_compute_region_network_policy.default {{name}}
```

## User Project Overrides

This resource supports [User Project Overrides](https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#user_project_override).
