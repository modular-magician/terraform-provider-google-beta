---
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This code is generated by Magic Modules using the following:
#
#     Configuration: https:#github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/products/networkservices/TelemetryPolicy.yaml
#     Template:      https:#github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/templates/terraform/resource.html.markdown.tmpl
#
#     DO NOT EDIT this file directly. Any changes made to this file will be
#     overwritten during the next generation cycle.
#
# ----------------------------------------------------------------------------
subcategory: "Network Services"
description: |-
  Provides a configuration for telemetry.
---

# google_network_services_telemetry_policy

Provides a configuration for telemetry.

~> **Warning:** This resource is in beta, and should be used with the terraform-provider-google-beta provider.
See [Provider Versions](../guides/provider_versions.html.markdown) for more details on beta resources.

To get more information about TelemetryPolicy, see:

* [API documentation](https://cloud.google.com/traffic-director/docs/reference/network-services/rest/v1beta1/projects.locations.telemetryPolicies)

<div class = "oics-button" style="float: right; margin: 0 0 -15px">
  <a href="https://console.cloud.google.com/cloudshell/open?cloudshell_git_repo=https%3A%2F%2Fgithub.com%2Fterraform-google-modules%2Fdocs-examples.git&cloudshell_image=gcr.io%2Fcloudshell-images%2Fcloudshell%3Alatest&cloudshell_print=.%2Fmotd&cloudshell_tutorial=.%2Ftutorial.md&cloudshell_working_dir=network_services_telemetry_policy_basic&open_in_editor=main.tf" target="_blank">
    <img alt="Open in Cloud Shell" src="//gstatic.com/cloudssh/images/open-btn.svg" style="max-height: 44px; margin: 32px auto; max-width: 100%;">
  </a>
</div>
## Example Usage - Network Services Telemetry Policy Basic


```hcl
resource "google_network_services_telemetry_policy" "default" {
  provider = google-beta
  name     = "my-telemetry-policy"
  location = "us-central1"
  labels   = {
    foo = "bar"
  }

  telemetry_target {
    resources = [
      "//staging-run.sandbox.googleapis.com/projects/PROJECT_NAME/locations/us-central1/services/my-service"
    ]
  }
}
```
<div class = "oics-button" style="float: right; margin: 0 0 -15px">
  <a href="https://console.cloud.google.com/cloudshell/open?cloudshell_git_repo=https%3A%2F%2Fgithub.com%2Fterraform-google-modules%2Fdocs-examples.git&cloudshell_image=gcr.io%2Fcloudshell-images%2Fcloudshell%3Alatest&cloudshell_print=.%2Fmotd&cloudshell_tutorial=.%2Ftutorial.md&cloudshell_working_dir=network_services_telemetry_policy_full&open_in_editor=main.tf" target="_blank">
    <img alt="Open in Cloud Shell" src="//gstatic.com/cloudssh/images/open-btn.svg" style="max-height: 44px; margin: 32px auto; max-width: 100%;">
  </a>
</div>
## Example Usage - Network Services Telemetry Policy Full


```hcl
resource "google_network_services_telemetry_policy" "default" {
  provider     = google-beta
  name         = "my-telemetry-policy-full"
  location     = "us-central1"
  display_name = "Full Telemetry Policy Sample"
  labels       = {
    foo = "bar"
  }

  telemetry_target {
    resources = [
      "//staging-run.sandbox.googleapis.com/projects/PROJECT_NAME/locations/us-central1/services/my-service"
    ]
  }

  tracing_configuration {
    sampling_rate = 0.5

    custom_span_attributes {
      attribute_name = "env"
      literal_value  = "prod"
    }

    custom_span_attributes {
      attribute_name    = "user-id"
      from_header_value = "x-user-id"
    }

    parent_based_sampling {
      enabled       = true
      sampling_rate = 0.8
    }
  }

  metrics_configuration {
    enabled = true
  }
}
```
<div class = "oics-button" style="float: right; margin: 0 0 -15px">
  <a href="https://console.cloud.google.com/cloudshell/open?cloudshell_git_repo=https%3A%2F%2Fgithub.com%2Fterraform-google-modules%2Fdocs-examples.git&cloudshell_image=gcr.io%2Fcloudshell-images%2Fcloudshell%3Alatest&cloudshell_print=.%2Fmotd&cloudshell_tutorial=.%2Ftutorial.md&cloudshell_working_dir=network_services_telemetry_policy_global&open_in_editor=main.tf" target="_blank">
    <img alt="Open in Cloud Shell" src="//gstatic.com/cloudssh/images/open-btn.svg" style="max-height: 44px; margin: 32px auto; max-width: 100%;">
  </a>
</div>
## Example Usage - Network Services Telemetry Policy Global


```hcl
resource "google_network_services_telemetry_policy" "default" {
  provider = google-beta
  name     = "my-telemetry-policy-global"
  location = "global"
  labels   = {
    foo = "bar"
  }

  telemetry_target {
    resources = [
      "//staging-run.sandbox.googleapis.com/projects/PROJECT_NAME/locations/us-central1/services/my-service"
    ]
  }
}
```

## Argument Reference

The following arguments are supported:


* `telemetry_target` -
  (Required)
  Specifies the set of targets to which this telemetry policy applies.
  Structure is [documented below](#nested_telemetry_target).

* `name` -
  (Required)
  Short name of the TelemetryPolicy resource to be created.


* `labels` -
  (Optional)
  Set of label tags associated with the TelemetryPolicy resource.

  **Note**: This field is non-authoritative, and will only manage the labels present in your configuration.
  Please refer to the field `effective_labels` for all of the labels present on the resource.

* `display_name` -
  (Optional)
  Provides a human-readable name for the policy.

* `tracing_configuration` -
  (Optional)
  Specifies the top-level configuration block within the telemetry policy that contains all the settings for how tracing will be applied to the target resources.
  Structure is [documented below](#nested_tracing_configuration).

* `metrics_configuration` -
  (Optional)
  Specifies the top-level configuration block within the telemetry policy that contains all the settings for how metrics will be applied to the target resources.
  Structure is [documented below](#nested_metrics_configuration).

* `location` -
  (Optional)
  Location (region) of the TelemetryPolicy resource to be created.

* `project` - (Optional) The ID of the project in which the resource belongs.
    If it is not provided, the provider project is used.

* `deletion_policy` - (Optional) Whether Terraform will be prevented from destroying the resource. Defaults to DELETE.
	When a 'terraform destroy' or 'terraform apply' would delete the resource,
	the command will fail if this field is set to "PREVENT" in Terraform state.
	When set to "ABANDON", the command will remove the resource from Terraform
	management without updating or deleting the resource in the API.
	When set to "DELETE", deleting the resource is allowed.


<a name="nested_telemetry_target"></a>The `telemetry_target` block supports:

* `resources` -
  (Required)
  Lists references to the resources that are targeted by the policy.

<a name="nested_tracing_configuration"></a>The `tracing_configuration` block supports:

* `sampling_rate` -
  (Optional)
  Specifies the tracing sampling rate.

* `custom_span_attributes` -
  (Optional)
  Provides key-value pairs for adding context to spans.
  Structure is [documented below](#nested_tracing_configuration_custom_span_attributes).

* `parent_based_sampling` -
  (Optional)
  Configures honoring a sampled bit in downstream traceparentID headers.
  Structure is [documented below](#nested_tracing_configuration_parent_based_sampling).


<a name="nested_tracing_configuration_custom_span_attributes"></a>The `custom_span_attributes` block supports:

* `attribute_name` -
  (Required)
  Defines the key for a custom attribute.

* `literal_value` -
  (Optional)
  Assigns a static, constant value to the custom trace span attribute.

* `from_header_value` -
  (Optional)
  Specifies an incoming request header name from which the value for the custom span attribute should be dynamically extracted.

<a name="nested_tracing_configuration_parent_based_sampling"></a>The `parent_based_sampling` block supports:

* `enabled` -
  (Optional)
  Provides a master switch to respect a sampled bit in incoming traceparentID headers.

* `sampling_rate` -
  (Optional)
  Specifies the sampling rate to use when the sampled bit is set in the incoming traceparentID header.

<a name="nested_metrics_configuration"></a>The `metrics_configuration` block supports:

* `enabled` -
  (Optional)
  Indicates whether metrics are enabled or disabled.

* `metadata` -
  (Optional)
  Can be used by an internal caller to propagate metadata.

## Attributes Reference

In addition to the arguments listed above, the following computed attributes are exported:

* `id` - an identifier for the resource with format `projects/{{project}}/locations/{{location}}/telemetryPolicies/{{name}}`

* `self_link` -
  Server-defined URL of this resource.

* `create_time` -
  Time the TelemetryPolicy was created in UTC.

* `update_time` -
  Time the TelemetryPolicy was updated in UTC.

* `uid` -
  Specifies the server-assigned unique identifier for the policy.

* `terraform_labels` -
  The combination of labels configured directly on the resource
   and default labels configured on the provider.

* `effective_labels` -
  All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services.


## Timeouts

This resource provides the following
[Timeouts](https://developer.hashicorp.com/terraform/plugin/sdkv2/resources/retries-and-customizable-timeouts) configuration options:

- `create` - Default is 30 minutes.
- `update` - Default is 30 minutes.
- `delete` - Default is 30 minutes.

## Import


TelemetryPolicy can be imported using any of these accepted formats:

* `projects/{{project}}/locations/{{location}}/telemetryPolicies/{{name}}`
* `{{project}}/{{location}}/{{name}}`
* `{{location}}/{{name}}`

In Terraform v1.12.0 and later, use an [`identity` block](https://developer.hashicorp.com/terraform/language/block/import#identity) to import TelemetryPolicy using identity values. For example:

```tf
import {
  identity = {
    name = "<-required value->"
    location = "<-optional value->"
    project = "<-optional value->"
  }
  to = google_network_services_telemetry_policy.default
}
```

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import) to import TelemetryPolicy using one of the formats above. For example:

```tf
import {
  id = "projects/{{project}}/locations/{{location}}/telemetryPolicies/{{name}}"
  to = google_network_services_telemetry_policy.default
}
```

When using the [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import), TelemetryPolicy can be imported using one of the formats above. For example:

```
$ terraform import google_network_services_telemetry_policy.default projects/{{project}}/locations/{{location}}/telemetryPolicies/{{name}}
$ terraform import google_network_services_telemetry_policy.default {{project}}/{{location}}/{{name}}
$ terraform import google_network_services_telemetry_policy.default {{location}}/{{name}}
```

## User Project Overrides

This resource supports [User Project Overrides](https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#user_project_override).
