---
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
#
# ----------------------------------------------------------------------------
#
#     This code is generated by Magic Modules using the following:
#
#     Configuration: https:#github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/products/designcenter/ApplicationTemplate.yaml
#     Template:      https:#github.com/GoogleCloudPlatform/magic-modules/tree/main/mmv1/templates/terraform/resource.html.markdown.tmpl
#
#     DO NOT EDIT this file directly. Any changes made to this file will be
#     overwritten during the next generation cycle.
#
# ----------------------------------------------------------------------------
subcategory: "Design Center"
description: |-
  Description
---

# google_design_center_application_template

Description



<div class = "oics-button" style="float: right; margin: 0 0 -15px">
  <a href="https://console.cloud.google.com/cloudshell/open?cloudshell_git_repo=https%3A%2F%2Fgithub.com%2Fterraform-google-modules%2Fdocs-examples.git&cloudshell_image=gcr.io%2Fcloudshell-images%2Fcloudshell%3Alatest&cloudshell_print=.%2Fmotd&cloudshell_tutorial=.%2Ftutorial.md&cloudshell_working_dir=designcenter_application_template_basic&open_in_editor=main.tf" target="_blank">
    <img alt="Open in Cloud Shell" src="//gstatic.com/cloudssh/images/open-btn.svg" style="max-height: 44px; margin: 32px auto; max-width: 100%;">
  </a>
</div>
## Example Usage - Designcenter Application Template Basic


```hcl
resource "google_design_center_application_template" "example" {
  location                = "us-central1"
  space                   = google_design_center_space.space.space_id
  application_template_id = "%{resource_name}"
  description             = "Test App Template Description"
  display_name            = "Test App Template Display Name"
}

resource "google_design_center_space" "space" {
  location     = "us-central1"
  space_id     = "space-%{resource_name}"
  description  = "Test Space Description"
  display_name = "Test Space Display Name"
}
```

## Argument Reference

The following arguments are supported:


* `location` -
  (Required)
  Resource ID segment making up resource `name`. It identifies the resource within its parent collection as described in https://google.aip.dev/122.

* `space` -
  (Required)
  Resource ID segment making up resource `name`. It identifies the resource within its parent collection as described in https://google.aip.dev/122.

* `application_template_id` -
  (Required)
  The ID of the application template to create.


* `application_parameters` -
  (Optional)
  Parameters to apply to all components in an application. You can specify
  projectID and region.
  Structure is [documented below](#nested_application_parameters).

* `artifact_location` -
  (Optional)
  Defines the location for storing an artifact, such as generated IaC.
  Structure is [documented below](#nested_artifact_location).

* `composition_type` -
  (Optional)
  Possible values:
  STANDARD
  COMPOSITE

* `description` -
  (Optional)
  Application template description.

* `display_name` -
  (Optional)
  Application template display name.

* `root_input_variables` -
  (Optional)
  Root level input variables of the application template.
  Structure is [documented below](#nested_root_input_variables).

* `root_output_variables` -
  (Optional)
  Root level output variables of the application template.
  Structure is [documented below](#nested_root_output_variables).

* `saas_runtime_context` -
  (Optional)
  SaaS runtime context.
  Structure is [documented below](#nested_saas_runtime_context).

* `project` - (Optional) The ID of the project in which the resource belongs.
    If it is not provided, the provider project is used.

* `deletion_policy` - (Optional) Whether Terraform will be prevented from destroying the resource. Defaults to DELETE.
	When a 'terraform destroy' or 'terraform apply' would delete the resource,
	the command will fail if this field is set to "PREVENT" in Terraform state.
	When set to "ABANDON", the command will remove the resource from Terraform
	management without updating or deleting the resource in the API.
	When set to "DELETE", deleting the resource is allowed.


<a name="nested_application_parameters"></a>The `application_parameters` block supports:

* `key` -
  (Required)
  The key of the parameter.

* `value` -
  (Optional)
  The value of the parameter.

* `value_expr` -
  (Output)
  The value of the parameter as a string.

<a name="nested_artifact_location"></a>The `artifact_location` block supports:

* `developer_connect_export_config` -
  (Optional)
  Through Developer Connect, defines a location where content such as
  Infrastructure as Code (IaC) is stored.
  Structure is [documented below](#nested_artifact_location_developer_connect_export_config).

* `gcs_uri` -
  (Optional)
  The Cloud Storage URI for storing the generated IaC.


<a name="nested_artifact_location_developer_connect_export_config"></a>The `developer_connect_export_config` block supports:

* `branch` -
  (Optional)
  The branch in repo to which the content such as Infrastructure as Code
  (IaC) should be written to. If empty, Application Design Center will create
  a branch and push the changes.

* `commit_sha` -
  (Output)
  The SHA of the Git commit that contains the exported content such as
  Infrastructure as Code (IaC).

* `developer_connect_repo_uri` -
  (Required)
  The Developer Connect Git repository link, formatted as
  `projects/*/locations/*/connections/*/gitRepositoryLinks/*`.

* `dir` -
  (Required)
  Directory, relative to the source repo, where content such as
  Infrastructure as Code (IaC) will be stored.
  This must be a relative path.To specify the root directory, use '/'.
  If the path or any subdirectories do not exist, they will be created.

<a name="nested_root_input_variables"></a>The `root_input_variables` block supports:

* `component_uri` -
  (Required)
  Component to which this variable belongs.

* `variable` -
  (Required)
  Name of the variable.

<a name="nested_root_output_variables"></a>The `root_output_variables` block supports:

* `component_uri` -
  (Required)
  Component to which this variable belongs.

* `variable` -
  (Required)
  Name of the variable.

<a name="nested_saas_runtime_context"></a>The `saas_runtime_context` block supports:

* `saas_names` -
  (Optional)
  The SaaS names. Format for each SaaS:
  projects/{project}/locations/{location}/saas/{saas}

## Attributes Reference

In addition to the arguments listed above, the following computed attributes are exported:

* `id` - an identifier for the resource with format `projects/{{project}}/locations/{{location}}/spaces/{{space}}/applicationTemplates/{{application_template_id}}`

* `create_time` -
  Application template creation timestamp.

* `iac_format` -
  The IaC format of the application template.
  Possible values:
  TERRAFORM
  HELM

* `latest_revision` -
  The latest application template revision.

* `latest_system_revision_uri` -
  The URI of the most recent System Revision generated for this
  ApplicationTemplate. This ID corresponds to the `name` field in the
  ApplicationTemplateSystemRevision resource.

* `name` -
  Identifier. Application template name.

* `serialized_application_template` -
  Serialized application template.
  Structure is [documented below](#nested_serialized_application_template).

* `update_time` -
  Application template update timestamp.


<a name="nested_serialized_application_template"></a>The `serialized_application_template` block contains:

* `apphub_application_parameters` -
  (Optional)
  App Hub application parameters.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters).

* `application_parameters` -
  (Optional)
  Parameters to apply to all components in the application template.
  Structure is [documented below](#nested_serialized_application_template_application_parameters).

* `components` -
  (Optional)
  The application template components.
  Structure is [documented below](#nested_serialized_application_template_components).

* `composition_type` -
  (Optional)
  Possible values:
  STANDARD
  COMPOSITE

* `description` -
  (Optional)
  The application template description.

* `display_name` -
  (Optional)
  The application template display name.

* `has_global_resource` -
  (Output)
  Whether the application template is compatible with regional scope.

* `iac_format` -
  (Optional)
  The IaC format of the application template.
  Possible values:
  TERRAFORM
  HELM

* `root_input_variables` -
  (Output)
  Root level input variables of the application template.
  Structure is [documented below](#nested_serialized_application_template_root_input_variables).

* `root_output_variables` -
  (Output)
  Root level output variables of the application template.
  Structure is [documented below](#nested_serialized_application_template_root_output_variables).

* `saas_runtime_context` -
  (Optional)
  SaaS runtime context.
  Structure is [documented below](#nested_serialized_application_template_saas_runtime_context).

* `serialized_policies` -
  (Output)
  Policies of the application template.
  Structure is [documented below](#nested_serialized_application_template_serialized_policies).

* `uri` -
  (Optional)
  The application template URI.


<a name="nested_serialized_application_template_apphub_application_parameters"></a>The `apphub_application_parameters` block supports:

* `application_id` -
  (Optional)
  The App Hub application ID.

* `attributes` -
  (Optional)
  Consumer provided attributes.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes).

* `host_project_id` -
  (Optional)
  The host project ID where the App Hub application is created.

* `location` -
  (Optional)
  The App Hub application location. Only used for applications with REGIONAL
  scope.

* `scope` -
  (Optional)
  Scope of an application.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_scope).


<a name="nested_serialized_application_template_apphub_application_parameters_attributes"></a>The `attributes` block supports:

* `business_owners` -
  (Optional)
  Business team that ensures user needs are met and value is delivered
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_business_owners).

* `criticality` -
  (Optional)
  Criticality of the Application, Service, or Workload
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_criticality).

* `developer_owners` -
  (Optional)
  Developer team that owns development and coding.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_developer_owners).

* `environment` -
  (Optional)
  Environment of the Application, Service, or Workload
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_environment).

* `operator_owners` -
  (Optional)
  Operator team that ensures runtime and operations.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_operator_owners).


<a name="nested_serialized_application_template_apphub_application_parameters_attributes_business_owners"></a>The `business_owners` block supports:

* `channel` -
  (Optional)
  Separate message to accommodate custom formats across IRC and Slack.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_business_owners_channel).

* `display_name` -
  (Optional)
  Contact's name.
  Can have a maximum length of 63 characters.

* `email` -
  (Required)
  Email address of the contacts.


<a name="nested_serialized_application_template_apphub_application_parameters_attributes_business_owners_channel"></a>The `channel` block supports:

* `uri` -
  (Required)
  URI of the channel.

<a name="nested_serialized_application_template_apphub_application_parameters_attributes_criticality"></a>The `criticality` block supports:

* `level` -
  (Optional)
  Criticality level.
  Can contain only lowercase letters, numeric characters,
  underscores, and dashes. Can have a maximum length of 63 characters.
  Deprecated: Please refer to type instead.

* `mission_critical` -
  (Optional)
  Indicates mission-critical Application, Service, or Workload.
  Deprecated: Please refer to type instead.

* `type` -
  (Required)
  Criticality Type.
  Possible values:
  MISSION_CRITICAL
  HIGH
  MEDIUM
  LOW

<a name="nested_serialized_application_template_apphub_application_parameters_attributes_developer_owners"></a>The `developer_owners` block supports:

* `channel` -
  (Optional)
  Separate message to accommodate custom formats across IRC and Slack.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_developer_owners_channel).

* `display_name` -
  (Optional)
  Contact's name.
  Can have a maximum length of 63 characters.

* `email` -
  (Required)
  Email address of the contacts.


<a name="nested_serialized_application_template_apphub_application_parameters_attributes_developer_owners_channel"></a>The `channel` block supports:

* `uri` -
  (Required)
  URI of the channel.

<a name="nested_serialized_application_template_apphub_application_parameters_attributes_environment"></a>The `environment` block supports:

* `environment` -
  (Optional)
  Environment name.
  Can contain only lowercase letters, numeric characters,
  underscores, and dashes. Can have a maximum length of 63 characters.
  Deprecated: Please refer to type instead.

* `type` -
  (Required)
  Environment Type.
  Possible values:
  PRODUCTION
  STAGING
  TEST
  DEVELOPMENT

<a name="nested_serialized_application_template_apphub_application_parameters_attributes_operator_owners"></a>The `operator_owners` block supports:

* `channel` -
  (Optional)
  Separate message to accommodate custom formats across IRC and Slack.
  Structure is [documented below](#nested_serialized_application_template_apphub_application_parameters_attributes_operator_owners_channel).

* `display_name` -
  (Optional)
  Contact's name.
  Can have a maximum length of 63 characters.

* `email` -
  (Required)
  Email address of the contacts.


<a name="nested_serialized_application_template_apphub_application_parameters_attributes_operator_owners_channel"></a>The `channel` block supports:

* `uri` -
  (Required)
  URI of the channel.

<a name="nested_serialized_application_template_apphub_application_parameters_scope"></a>The `scope` block supports:

* `type` -
  (Required)
  Scope Type.
  Possible values:
  REGIONAL
  GLOBAL

<a name="nested_serialized_application_template_application_parameters"></a>The `application_parameters` block supports:

* `key` -
  (Required)
  The key of the parameter.

* `value` -
  (Optional)
  The value of the parameter.

* `value_expr` -
  (Output)
  The value of the parameter as a string.

<a name="nested_serialized_application_template_components"></a>The `components` block supports:

* `apis` -
  (Optional)
  APIs required to be enabled to deploy the component, in the form
  of "*.googleapis.com".

* `application_info` -
  (Optional)
  This captures the apphub application details associated with the component.
  Structure is [documented below](#nested_serialized_application_template_components_application_info).

* `component_parameter_schema` -
  (Output)
  The component parameter schema, which includes possible parameter values.
  Structure is [documented below](#nested_serialized_application_template_components_component_parameter_schema).

* `connections` -
  (Optional)
  The component connections.
  Structure is [documented below](#nested_serialized_application_template_components_connections).

* `connections_parameters` -
  (Output)
  The connection parameters of the component.
  Structure is [documented below](#nested_serialized_application_template_components_connections_parameters).

* `display_name` -
  (Optional)
  The component display name.

* `parameters` -
  (Optional)
  The component parameters.
  Structure is [documented below](#nested_serialized_application_template_components_parameters).

* `roles` -
  (Optional)
  IAM roles required by the service account to deploy the component.

* `shared_template_revision_uri` -
  (Optional)
  The shared template used to generate the component.

* `uri` -
  (Optional)
  The component URI.


<a name="nested_serialized_application_template_components_application_info"></a>The `application_info` block supports:

* `apphub_application_id` -
  (Required)
  The application ID of the apphub application. The ID must be 1-63
  characters long and should match the regular expression
  `^[a-z]([a-z0-9-]{0,61}[a-z0-9])?$`.

* `attributes` -
  (Optional)
  Consumer provided attributes.
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes).

* `deployment_region` -
  (Optional)
  Deployment region for the component. If the scope is set to REGIONAL,
  then the apphub application is created in this region, e.g.
  `us-central1`.

* `display_name` -
  (Optional)
  Display name for the application. The number of characters should be less
  than 64 characters.

* `scope` -
  (Optional)
  Scope of an application.
  Structure is [documented below](#nested_serialized_application_template_components_application_info_scope).


<a name="nested_serialized_application_template_components_application_info_attributes"></a>The `attributes` block supports:

* `business_owners` -
  (Optional)
  Business team that ensures user needs are met and value is delivered
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_business_owners).

* `criticality` -
  (Optional)
  Criticality of the Application, Service, or Workload
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_criticality).

* `developer_owners` -
  (Optional)
  Developer team that owns development and coding.
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_developer_owners).

* `environment` -
  (Optional)
  Environment of the Application, Service, or Workload
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_environment).

* `operator_owners` -
  (Optional)
  Operator team that ensures runtime and operations.
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_operator_owners).


<a name="nested_serialized_application_template_components_application_info_attributes_business_owners"></a>The `business_owners` block supports:

* `channel` -
  (Optional)
  Separate message to accommodate custom formats across IRC and Slack.
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_business_owners_channel).

* `display_name` -
  (Optional)
  Contact's name.
  Can have a maximum length of 63 characters.

* `email` -
  (Required)
  Email address of the contacts.


<a name="nested_serialized_application_template_components_application_info_attributes_business_owners_channel"></a>The `channel` block supports:

* `uri` -
  (Required)
  URI of the channel.

<a name="nested_serialized_application_template_components_application_info_attributes_criticality"></a>The `criticality` block supports:

* `level` -
  (Optional)
  Criticality level.
  Can contain only lowercase letters, numeric characters,
  underscores, and dashes. Can have a maximum length of 63 characters.
  Deprecated: Please refer to type instead.

* `mission_critical` -
  (Optional)
  Indicates mission-critical Application, Service, or Workload.
  Deprecated: Please refer to type instead.

* `type` -
  (Required)
  Criticality Type.
  Possible values:
  MISSION_CRITICAL
  HIGH
  MEDIUM
  LOW

<a name="nested_serialized_application_template_components_application_info_attributes_developer_owners"></a>The `developer_owners` block supports:

* `channel` -
  (Optional)
  Separate message to accommodate custom formats across IRC and Slack.
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_developer_owners_channel).

* `display_name` -
  (Optional)
  Contact's name.
  Can have a maximum length of 63 characters.

* `email` -
  (Required)
  Email address of the contacts.


<a name="nested_serialized_application_template_components_application_info_attributes_developer_owners_channel"></a>The `channel` block supports:

* `uri` -
  (Required)
  URI of the channel.

<a name="nested_serialized_application_template_components_application_info_attributes_environment"></a>The `environment` block supports:

* `environment` -
  (Optional)
  Environment name.
  Can contain only lowercase letters, numeric characters,
  underscores, and dashes. Can have a maximum length of 63 characters.
  Deprecated: Please refer to type instead.

* `type` -
  (Required)
  Environment Type.
  Possible values:
  PRODUCTION
  STAGING
  TEST
  DEVELOPMENT

<a name="nested_serialized_application_template_components_application_info_attributes_operator_owners"></a>The `operator_owners` block supports:

* `channel` -
  (Optional)
  Separate message to accommodate custom formats across IRC and Slack.
  Structure is [documented below](#nested_serialized_application_template_components_application_info_attributes_operator_owners_channel).

* `display_name` -
  (Optional)
  Contact's name.
  Can have a maximum length of 63 characters.

* `email` -
  (Required)
  Email address of the contacts.


<a name="nested_serialized_application_template_components_application_info_attributes_operator_owners_channel"></a>The `channel` block supports:

* `uri` -
  (Required)
  URI of the channel.

<a name="nested_serialized_application_template_components_application_info_scope"></a>The `scope` block supports:

* `type` -
  (Required)
  Scope Type.
  Possible values:
  REGIONAL
  GLOBAL

<a name="nested_serialized_application_template_components_component_parameter_schema"></a>The `component_parameter_schema` block contains:

* `default_value` -
  (Output)
  The default value of the parameter.

* `is_required` -
  (Output)
  Whether the parameter is required.

* `key` -
  (Output)
  The key of the parameter.

* `type` -
  (Output)
  The type of the parameter.

<a name="nested_serialized_application_template_components_connections"></a>The `connections` block supports:

* `destination_component_parameters` -
  (Optional)
  The parameters of the connection associated with the destination component.
  Structure is [documented below](#nested_serialized_application_template_components_connections_destination_component_parameters).

* `destination_component_uri` -
  (Optional)
  The destination component URI used to generate the connection.

* `source_component_parameters` -
  (Optional)
  The parameters of the connection associated with the source component.
  Structure is [documented below](#nested_serialized_application_template_components_connections_source_component_parameters).

* `uri` -
  (Optional)
  The connection URI.


<a name="nested_serialized_application_template_components_connections_destination_component_parameters"></a>The `destination_component_parameters` block supports:

* `key` -
  (Required)
  The key of the parameter.

* `value` -
  (Optional)
  The value of the parameter.

* `value_expr` -
  (Output)
  The value of the parameter as a string.

<a name="nested_serialized_application_template_components_connections_source_component_parameters"></a>The `source_component_parameters` block supports:

* `key` -
  (Required)
  The key of the parameter.

* `value` -
  (Optional)
  The value of the parameter.

* `value_expr` -
  (Output)
  The value of the parameter as a string.

<a name="nested_serialized_application_template_components_connections_parameters"></a>The `connections_parameters` block contains:

* `connection` -
  (Output)
  The name of the connection parameter.

* `parameters` -
  (Output)
  A list of parameters associated with the connection.
  Structure is [documented below](#nested_serialized_application_template_components_connections_parameters_parameters).


<a name="nested_serialized_application_template_components_connections_parameters_parameters"></a>The `parameters` block contains:

* `key` -
  (Output)
  The key of the parameter.

* `value` -
  (Output)
  The value of the parameter.

* `value_expr` -
  (Output)
  The value of the parameter as a string.

<a name="nested_serialized_application_template_components_parameters"></a>The `parameters` block supports:

* `key` -
  (Required)
  The key of the parameter.

* `value` -
  (Optional)
  The value of the parameter.

* `value_expr` -
  (Output)
  The value of the parameter as a string.

<a name="nested_serialized_application_template_root_input_variables"></a>The `root_input_variables` block contains:

* `component_uri` -
  (Output)
  Component to which this variable belongs.

* `variable` -
  (Output)
  Name of the variable.

<a name="nested_serialized_application_template_root_output_variables"></a>The `root_output_variables` block contains:

* `component_uri` -
  (Output)
  Component to which this variable belongs.

* `variable` -
  (Output)
  Name of the variable.

<a name="nested_serialized_application_template_saas_runtime_context"></a>The `saas_runtime_context` block supports:

* `saas_names` -
  (Optional)
  The SaaS names. Format for each SaaS:
  projects/{project}/locations/{location}/saas/{saas}

<a name="nested_serialized_application_template_serialized_policies"></a>The `serialized_policies` block contains:

* `apply_conditions` -
  (Output)
  The serialized policy apply conditions.
  Structure is [documented below](#nested_serialized_application_template_serialized_policies_apply_conditions).

* `description` -
  (Output)
  The serialized policy description.
  At most 2048 characters.

* `display_name` -
  (Output)
  The serialized policy display name.
  The number of characters should be less than 128 characters.

* `policy_revision_uri` -
  (Output)
  URI of the policy revision.

* `policy_type` -
  (Output)
  Policy type will be used to determine the domain of the policy.
  Possible values:
  COMPLIANCE_FRAMEWORK

* `policy_uri` -
  (Output)
  URI of the policy.

* `uri` -
  (Output)
  The uri of the policy in application template.
  Format:
  `projects/{project}/locations/{location}/spaces/{space}/applicationTemplates/{application_template}/policies/{policy}`


<a name="nested_serialized_application_template_serialized_policies_apply_conditions"></a>The `apply_conditions` block contains:

* `condition_key` -
  (Output)
  The key of the parameter for the condition.

* `condition_value` -
  (Output)
  The value of the parameter for the condition.

## Timeouts

This resource provides the following
[Timeouts](https://developer.hashicorp.com/terraform/plugin/sdkv2/resources/retries-and-customizable-timeouts) configuration options:

- `create` - Default is 20 minutes.
- `update` - Default is 20 minutes.
- `delete` - Default is 20 minutes.

## Import


ApplicationTemplate can be imported using any of these accepted formats:

* `projects/{{project}}/locations/{{location}}/spaces/{{space}}/applicationTemplates/{{application_template_id}}`
* `{{project}}/{{location}}/{{space}}/{{application_template_id}}`
* `{{location}}/{{space}}/{{application_template_id}}`

In Terraform v1.12.0 and later, use an [`identity` block](https://developer.hashicorp.com/terraform/language/block/import#identity) to import ApplicationTemplate using identity values. For example:

```tf
import {
  identity = {
    location = "<-required value->"
    space = "<-required value->"
    applicationTemplateId = "<-required value->"
    project = "<-optional value->"
  }
  to = google_design_center_application_template.default
}
```

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import) to import ApplicationTemplate using one of the formats above. For example:

```tf
import {
  id = "projects/{{project}}/locations/{{location}}/spaces/{{space}}/applicationTemplates/{{application_template_id}}"
  to = google_design_center_application_template.default
}
```

When using the [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import), ApplicationTemplate can be imported using one of the formats above. For example:

```
$ terraform import google_design_center_application_template.default projects/{{project}}/locations/{{location}}/spaces/{{space}}/applicationTemplates/{{application_template_id}}
$ terraform import google_design_center_application_template.default {{project}}/{{location}}/{{space}}/{{application_template_id}}
$ terraform import google_design_center_application_template.default {{location}}/{{space}}/{{application_template_id}}
```

## User Project Overrides

This resource supports [User Project Overrides](https://registry.terraform.io/providers/hashicorp/google/latest/docs/guides/provider_reference#user_project_override).
