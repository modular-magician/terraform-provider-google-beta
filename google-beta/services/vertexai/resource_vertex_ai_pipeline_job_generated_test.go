// Copyright IBM Corp. 2014, 2026
// SPDX-License-Identifier: MPL-2.0

// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    Type: MMv1     ***
//
// ----------------------------------------------------------------------------
//
//     This file is automatically generated by Magic Modules and manual
//     changes will be clobbered when the file is regenerated.
//
//     Please read more about how to change this file in
//     .github/CONTRIBUTING.md.
//
// ----------------------------------------------------------------------------

package vertexai_test

import (
	"fmt"
	"log"
	"strconv"
	"strings"
	"testing"
	"time"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
	"github.com/hashicorp/terraform-plugin-testing/terraform"

	"github.com/hashicorp/terraform-provider-google-beta/google-beta/acctest"
	"github.com/hashicorp/terraform-provider-google-beta/google-beta/envvar"
	"github.com/hashicorp/terraform-provider-google-beta/google-beta/services/vertexai"
	"github.com/hashicorp/terraform-provider-google-beta/google-beta/tpgresource"
	transport_tpg "github.com/hashicorp/terraform-provider-google-beta/google-beta/transport"

	"google.golang.org/api/googleapi"
)

var (
	_ = fmt.Sprintf
	_ = log.Print
	_ = strconv.Atoi
	_ = strings.Trim
	_ = time.Now
	_ = resource.TestMain
	_ = terraform.NewState
	_ = envvar.TestEnvVar
	_ = tpgresource.SetLabels
	_ = transport_tpg.Config{}
	_ = googleapi.Error{}
	_ = vertexai.Product
)

func TestAccVertexAIPipelineJob_vertexAiPipelineJobMinExample(t *testing.T) {
	t.Parallel()

	randomSuffix := acctest.RandString(t, 10)

	context := map[string]interface{}{
		"org_id":        envvar.GetTestOrgFromEnv(t),
		"project":       envvar.GetTestProjectFromEnv(),
		"random_suffix": randomSuffix,
	}

	acctest.VcrTest(t, resource.TestCase{
		PreCheck:                 func() { acctest.AccTestPreCheck(t) },
		ProtoV5ProviderFactories: acctest.ProtoV5ProviderBetaFactories(t),
		CheckDestroy:             testAccCheckVertexAIPipelineJobDestroyProducer(t),
		Steps: []resource.TestStep{
			{
				Config: testAccVertexAIPipelineJob_vertexAiPipelineJobMinExample(context),
			},
			{
				ResourceName:            "google_vertex_ai_pipeline_job.pipelinejob_min",
				ImportState:             true,
				ImportStateVerify:       true,
				ImportStateVerifyIgnore: []string{"labels", "region", "terraform_labels"},
			},
			{
				ResourceName:       "google_vertex_ai_pipeline_job.pipelinejob_min",
				RefreshState:       true,
				ExpectNonEmptyPlan: true,
				ImportStateKind:    resource.ImportBlockWithResourceIdentity,
			},
		},
	})
}

func testAccVertexAIPipelineJob_vertexAiPipelineJobMinExample(context map[string]interface{}) string {
	return acctest.Nprintf(`
resource "google_compute_network" "vpc_net" {
  provider                = google-beta
  project                 = "%{project}"
  name                    = "tf-test-vpc-%{random_suffix}"
  auto_create_subnetworks = false
}

resource "google_compute_subnetwork" "subnetwork" {
  provider      = google-beta
  project       = "%{project}"
  name          = "tf-test-subnet-%{random_suffix}"
  ip_cidr_range = "10.0.0.0/24"
  region        = "us-east1"
  network       = google_compute_network.vpc_net.id
}

resource "google_compute_network_attachment" "net_attachment" {
  provider              = google-beta
  project               = "%{project}"
  region                = "us-east1"
  name                  = "tf-test-net-att-%{random_suffix}"
  connection_preference = "ACCEPT_AUTOMATIC"
  subnetworks           = [google_compute_subnetwork.subnetwork.id]
}

resource "google_compute_global_address" "reserved_ip" {
  provider      = google-beta
  project       = "%{project}"
  name          = "tf-test-reserved-ip-%{random_suffix}"
  purpose       = "VPC_PEERING"
  address_type  = "INTERNAL"
  prefix_length = 24
  network       = google_compute_network.vpc_net.id
}

resource "google_service_account" "pipeline_sa" {
  provider     = google-beta
  account_id   = "pipeline-owner-sa-%{random_suffix}"
  project      = "%{project}"
  display_name = "Pipeline Owner Service Account"
}

resource "google_vertex_ai_metadata_store" "default_test" {
  provider = google-beta
  name     = "default-new-%{random_suffix}"
  project  = "%{project}"
  region   = "us-east1"
}

resource "google_project_iam_member" "pipeline_sa_roles" {
  provider = google-beta
  project  = "%{project}"
  role     = "roles/aiplatform.admin"
  member   = "serviceAccount:${google_service_account.pipeline_sa.email}"
}

resource "google_storage_bucket" "pipeline_output" {
  provider = google-beta
  project  = "%{project}"
  name     = "tf-test-pipeline-output-%{random_suffix}"
  location = "US"
}

resource "google_vertex_ai_pipeline_job" "primary" {
  provider     = google-beta
  project      = "%{project}"
  region       = "us-east1"
  display_name = "tf-test-example-pipeline-job%{random_suffix}"
  template_uri = "gs://cloud-samples-data/ai-platform/pipelines/hello-world-pipeline.json"
  
  labels = {
    env  = "test"
    tier = "ml"
  }

  network = google_compute_network.vpc_net.id

  psc_interface_config {
    network_attachment = google_compute_network_attachment.net_attachment.id
  }

  reserved_ip_ranges = [
    google_compute_global_address.reserved_ip.name
  ]

  preflight_validations = true

  runtime_config {
    gcs_output_directory = "gs://${google_storage_bucket.pipeline_output.name}/outputs/"
  }

  service_account = google_service_account.pipeline_sa.email

  depends_on = [
    google_project_iam_member.pipeline_sa_roles,
    google_vertex_ai_metadata_store.default_test,
    google_compute_network_attachment.net_attachment
  ]
}

resource "google_vertex_ai_pipeline_job" "clone" {
  provider     = google-beta
  project      = "%{project}"
  region       = "us-east1"
  display_name = "tf-test-clone-job%{random_suffix}"

  original_pipeline_job_id = google_vertex_ai_pipeline_job.primary.id

  template_uri = "gs://cloud-samples-data/ai-platform/pipelines/hello-world-pipeline.json"

  runtime_config {
    gcs_output_directory = "gs://${google_storage_bucket.pipeline_output.name}/outputs/"
  }

  service_account = google_service_account.pipeline_sa.email

  depends_on = [
    google_vertex_ai_pipeline_job.primary
  ]
}

resource "google_vertex_ai_pipeline_job" "with_spec" {
  provider     = google-beta
  project      = "%{project}"
  region       = "us-east1"
  display_name = "tf-test-spec-job%{random_suffix}"

  pipeline_spec = jsonencode({
    pipelineInfo = {
      name = "minimal-test-pipeline"
    }
    root = {
      dag = {
        tasks = {}
      }
    }
  })

  runtime_config {
    gcs_output_directory = "gs://${google_storage_bucket.pipeline_output.name}/outputs/"
  }

  service_account = google_service_account.pipeline_sa.email

  depends_on = [
    google_project_iam_member.pipeline_sa_roles
  ]
}
`, context)
}

func testAccCheckVertexAIPipelineJobDestroyProducer(t *testing.T) func(s *terraform.State) error {
	return func(s *terraform.State) error {
		for name, rs := range s.RootModule().Resources {
			if rs.Type != "google_vertex_ai_pipeline_job" {
				continue
			}
			if strings.HasPrefix(name, "data.") {
				continue
			}

			config := acctest.GoogleProviderConfig(t)
			url, err := tpgresource.ReplaceVarsForTest(config, rs, fmt.Sprintf("%s%s", transport_tpg.BaseUrl(vertexai.Product, config), "projects/{{project}}/locations/{{region}}/pipelineJobs/{{name}}"))
			if err != nil {
				return err
			}

			billingProject := ""

			if config.BillingProject != "" {
				billingProject = config.BillingProject
			}

			_, err = transport_tpg.SendRequest(transport_tpg.SendRequestOptions{
				Config:    config,
				Method:    "GET",
				Project:   billingProject,
				RawURL:    url,
				UserAgent: config.UserAgent,
			})
			if err == nil {
				return fmt.Errorf("VertexAIPipelineJob still exists at %s", url)
			}
		}

		return nil
	}
}
